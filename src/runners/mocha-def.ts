

export const beforeAll = ( content: string ) => `before( ${content} );`;

export const afterAll = ( content: string ) => `after( ${content} );`;

export const beforeEach = ( content: string ) => `beforeEach( ${content} );`;

export const afterEach = ( content: string ) => `afterEach( ${content} );`;