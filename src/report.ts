import { TestScriptExecutionResult } from 'concordialang-types';

export async function convert(filePath: string): Promise< TestScriptExecutionResult > {
    console.log( '[[ TO-DO: convert report with Playwright ]]');
    const r = {} as TestScriptExecutionResult;
    return r;
}